# AGENTS.md

このディレクトリは、LDAP Search Filter (RFC 4515) の文字列表現を読み取り、CSV/LTSV のログ行をフィルタして表示する実装を、複数の言語で試すための場所です。

## 目的

- RFC 4515 のフィルタ式を解釈できること
- 入力ログの各行を評価し、条件に一致した行だけを出力すること
- できるだけ各言語の標準機能で実装し、挙動を比較しやすくすること

## 参照実装

- `ruby/ldap_filter.rb` をサンプル実装として扱います
- 新しい実装は、まずこの Ruby 版の振る舞いを基準にしてください
- 仕様に迷ったら、`rfc4515.txt` と Ruby 実装の両方を確認してください

## 共通の振る舞い

- フィルタ式は `(` `)` を含む LDAP 形式で受け取ります
- 論理演算子 `&` `|` `!` を扱います
- 比較演算子 `=` `~=` `>=` `<=` を扱います
- `=` の値に `*` が含まれる場合はワイルドカードとして扱います
- `*` 単体は presence filter として扱います
- 入力行は 1 行ごとに評価し、一致した行だけを表示します

## 実装上の注意

- Ruby サンプルと互換のある出力・判定を優先してください
- 不要な抽象化は避け、まず読みやすさを優先してください
- 依存ライブラリは最小限にしてください
- 文字列のエスケープや UTF-8 の扱いは丁寧に確認してください
- 数値比較や近似一致の解釈は言語差が出やすいので、実装時に明示してください

## ベンチマーク設計指針

- ベンチマークは「処理速度の目安」を主目的にし、処理系の起動時間はできるだけ切り離してください
- CLI 全体の実時間は driver が外側から測り、比較の主指標にはしないでください
- `stderr` には診断用のフェーズ時刻を出してください
- 1 行 1 レコードの `key=value` 形式を基本にしてください
- 時刻は wall clock ではなく単調増加時間を使ってください
- 計測単位は `ns` か `ms` に統一してください。精度を優先するなら `ns` を推奨します
- `boot` は処理系起動やランタイム初期化の区間です。これは比較対象から外します
- `ready` は「入力処理を始められる状態になった時点」です。ベンチの開始点はここに置いてください
- `done` は「入力を読み終えて出力を終えた時点」です。ベンチの終了点はここに置いてください
- 必要なら `parse_filter` `read_input` `match_rows` `emit_rows` のような補助フェーズを増やしてください
- driver は `ready` から `done` までを処理時間の目安として扱い、`boot` は別情報として残してください

### 推奨ログ形式

```text
phase=boot t=1234567890 elapsed_ns=120000000
phase=ready t=1354567890 elapsed_ns=240000000
phase=match_rows t=1454567890 elapsed_ns=340000000
phase=done t=1554567890 elapsed_ns=440000000
```

- `t` はプロセス開始からの単調増加時刻です
- `elapsed_ns` はプロセス開始からの経過時間です
- `phase=ready` 以降を処理性能の比較対象にしてください
- Java のように起動が重い処理系でも、`ready` 以降の値が見えるようにしてください

## 変更時の確認

- 既存の言語実装がある場合は、その流儀に合わせてください
- 新しい言語を追加する場合は、ディレクトリ構成をわかりやすく保ってください
- 仕様変更があるときは、Ruby サンプルとの差分が分かるように記録してください

## 作業メモ

- ファイルを増やすときは、言語ごとの実装が探しやすい配置にしてください
- テストやサンプル入力があるなら、実装と同じ場所か分かりやすい場所に置いてください
- 迷ったら、まず Ruby 実装に合わせ、それから必要最小限の改善をしてください
