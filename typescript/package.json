{
  "name": "ldf-typescript",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.js",
    "start": "node dist/cli.js"
  }
}
